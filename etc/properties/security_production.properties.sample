MemcachedEndpoints=localhost:11211

default.failure.url=/auth/login?error=true
session.validity.seconds=600
singleuse.token.validity.seconds=300

password.pattern=^\\S{6,}$

#Enable or disable SSL.  Valid values are: http, https, any
requires.channel=https